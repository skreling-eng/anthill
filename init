#!/usr/bin/env bash
# Fresh checkout: install deps + external venvs (models: tools/download_all_models.sh).
#   ./init
#   ./init --skip-sage
set -euo pipefail
Root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$Root/tools/init.sh" "$@"
