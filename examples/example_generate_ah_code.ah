

@clear_answer:
Return only the final result without comments, clarifications, or explanations.

@ah_code: {
    @clear_answer
    -> $file('_lang_desc', source_path=True)
    -> $folder('examples', source_path=True)
    -> $code -> $ah
}
Create a script in Anthill to generate an image clip about rabbits.
Don't use $ah for the top level tasks. Only for subtasks.

run @ah_code