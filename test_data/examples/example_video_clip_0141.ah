# Request: Use $video_clip to assemble a clip about a coral garden aquarium. (variant 141)

@get_clip: $file('test_data/clip/demo-poster.png')
  -> $folder('test_data/clip', source_path=True)
  -> $video_clip
  -> $output

run @get_clip
