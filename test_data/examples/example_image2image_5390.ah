# Request: Apply a a deep cave spelunker headlamp look to the input photo. (variant 5390)

@load: $file('test_data/clip/demo-poster.png')

@edit: @load -> $image2image(model='default', steps=10)
Make the image evoke a deep cave spelunker headlamp. Keep the main subject.
Return only the final result without comments, clarifications, or explanations.

run @edit
