# Request: Transform the loaded image toward a mushroom forager's basket. (variant 5493)

@load: $file('test_data/same_person/1/9.png')

@edit: @load -> $image2image(model='sfw-v23', steps=12)
Make the image evoke a mushroom forager's basket. Keep the main subject.
Return only the final result without comments, clarifications, or explanations.

run @edit
