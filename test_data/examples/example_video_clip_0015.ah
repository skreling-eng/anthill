# Request: Combine folder images and audio into a video clip about a snow leopard on a ridge.

@get_clip: $file('test_data/clip/demo-poster.png')
  -> $folder('test_data/clip', source_path=True)
  -> $video_clip
  -> $output

run @get_clip
