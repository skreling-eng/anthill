# Request: Build a video clip pipeline for a driftwood beach sculpture. (variant 157)

@get_clip: $file('test_data/clip/demo-poster.png')
  -> $folder('test_data/clip', source_path=True)
  -> $video_clip
  -> $output

run @get_clip
