# Request: Merge images and wav into a clip illustrating a choir in a Gothic nave.

@get_clip: $file('test_data/clip/demo-poster.png')
  -> $folder('test_data/clip', source_path=True)
  -> $video_clip
  -> $output

run @get_clip
