# Request: Produce an audio track themed on a mushroom forager's basket. (variant 408)

@music_style: $list
orchestral

@lyrics_text: $llm
Write lyrics for a song about a mushroom forager's basket.
Return only the final result without comments, clarifications, or explanations.

@track: (@music_style, @lyrics_text) -> $music(model='st', guidance_scale=4.0, vocal_language='en', inference_steps=50)

run @track
