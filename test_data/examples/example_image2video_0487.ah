# Request: I need a video sequence depicting a canoe on a misty lake. (variant 487)

@prompts: $llm -> $texts_to_prompts
Still frame of a canoe on a misty lake, photorealistic, sharp detail.
Return only the prompt, under 50 words.

@image: @prompts -> $image(model='default')

@video: @image -> $image2video(model='default', width=768, height=512, frames=49, steps=4, guidance=1)
fluid motion, dynamic framing, high detail
Return only the final result without comments, clarifications, or explanations.

run @video
