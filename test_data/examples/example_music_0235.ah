# Request: Create a song about a robot assembling flowers. (variant 235)

@music_style: $list
electronic dance

@lyrics_text: $llm
Write lyrics for a song about a robot assembling flowers.
Return only the final result without comments, clarifications, or explanations.

@track: (@music_style, @lyrics_text) -> $music(model='st', guidance_scale=4.0, vocal_language='en', inference_steps=50)

run @track
