# Request: Use $image_clip for a video about underwater coral reef turtles. (variant 368)

@quality: $list
High resolution, best quality, photorealistic, cinematic

@clip_images: @quality -> $llm -> $texts_to_prompts -> $image(model='default')[6]
Create image prompts for a short clip about underwater coral reef turtles.
Return only the final result without comments, clarifications, or explanations.

@music_style: $list
folk

@lyrics_text: $llm
Write short song lyrics about underwater coral reef turtles.
Return only the final result without comments, clarifications, or explanations.

@track: (@music_style, @lyrics_text) -> $music(model='st', guidance_scale=4.0, vocal_language='en', inference_steps=50)

@gen_clip: (@clip_images, @track) -> $image_clip -> $output

run @gen_clip
