# Request: Return JSON with keys topic, summary, difficulty for: bird watching.

@answer: $llm
Return JSON with keys topic, summary, difficulty for: bird watching.
Return only valid JSON, no markdown.
Return only the final result without comments, clarifications, or explanations.

run @answer
