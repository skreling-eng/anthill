# Request: Make the photo look like editorial magazine quality.

@load: $file('test_data/abnormal_images/20260530_025631_default_image_0.png')

@edit: @load -> $image2image(model='sfw-v23', steps=6)
Make the photo look like editorial magazine quality.
Return only the final result without comments, clarifications, or explanations.

run @edit
